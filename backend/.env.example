# Backend Environment
PORT=4000
# Atlas connection (recommended)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/bookseller?retryWrites=true&w=majority
# Set to true to use local MongoDB if installed
USE_LOCAL_MONGO=false
# Optional local URI override (default: mongodb://127.0.0.1:27017/bookseller)
MONGO_LOCAL_URI=
# Demo mode (mock APIs). Keep false for real DB.
DEMO_MODE=false
